export function textStats(value=''){const text=String(value),trim=text.trim();const chars=text.length,charsNoSpace=text.replace(/\s/g,'').length,words=trim?trim.split(/\s+/).length:0,lines=text?text.split(/\r?\n/).length:0;return{chars,charsNoSpace,words,lines}}
export function normalizeSpaces(value=''){return String(value).split(/\r?\n/).map(line=>line.replace(/[ \t]+/g,' ').trimEnd()).join('\n').replace(/\n{3,}/g,'\n\n')}
export function transformText(value='',mode='none'){const s=String(value);if(mode==='upper')return s.toUpperCase();if(mode==='lower')return s.toLowerCase();if(mode==='trim')return normalizeSpaces(s).trim();if(mode==='title')return s.replace(/\b\w/g,c=>c.toUpperCase());return s}
export function replaceText(value='',find='',replacement='',replaceAll=true){const text=String(value),needle=String(find);if(!needle)return{text,count:0};if(replaceAll){const parts=text.split(needle);return{text:parts.join(String(replacement)),count:Math.max(0,parts.length-1)}}const index=text.indexOf(needle);if(index<0)return{text,count:0};return{text:text.slice(0,index)+String(replacement)+text.slice(index+needle.length),count:1}}
export function uniqueLines(value='',options={}){const keepEmpty=options.keepEmpty!==false,seen=new Set(),out=[];let removed=0;for(const line of String(value).split(/\r?\n/)){const key=line.trim();if(!keepEmpty&&!key){removed++;continue}if(seen.has(key)){removed++;continue}seen.add(key);out.push(line)}return{text:out.join('\n'),removed}}
export function sortLines(value='',direction='asc'){const lines=String(value).split(/\r?\n/);const sign=direction==='desc'?-1:1;lines.sort((a,b)=>a.localeCompare(b,undefined,{numeric:true,sensitivity:'base'})*sign);return lines.join('\n')}
export async function copyText(value=''){const text=String(value);if(navigator.clipboard?.writeText){await navigator.clipboard.writeText(text);return true}const ta=document.createElement('textarea');ta.value=text;ta.style.position='fixed';ta.style.opacity='0';document.body.appendChild(ta);ta.select();const ok=document.execCommand('copy');ta.remove();return ok}
