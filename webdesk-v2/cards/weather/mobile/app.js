import {loadWeather,weatherIcon,weatherLabel} from '../shared/weather-service.js';
export async function mount({host,signal}){host.innerHTML='<section class="weather-app weather-app-mobile"><p>載入天氣中…</p></section>';const d=await loadWeather({signal});if(signal.aborted)return;host.innerHTML=`<section class="weather-app weather-app-mobile"><div class="weather-mobile-current"><small>${d.place}</small><h2>${weatherIcon(d.code)} ${d.temperature}°</h2><p>${weatherLabel(d.code)} · 體感 ${d.feelsLike}°</p><div><span>↑ ${d.high}°</span><span>↓ ${d.low}°</span><span>雨 ${d.rain}%</span></div></div><div class="weather-mobile-list">${d.forecast.map(x=>`<article><span>${x.label}</span><strong>${weatherIcon(x.code)}</strong><b>${x.high}°</b><small>${x.low}°</small></article>`).join('')}</div></section>`}
