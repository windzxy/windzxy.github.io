import {loadWeather,weatherIcon,weatherLabel} from '../shared/weather-service.js';
export async function mount({host,sdk,signal}){host.innerHTML='<section class="weather-v2 weather-v2-mobile" aria-busy="true"></section>';const d=await loadWeather({signal});if(signal?.aborted)return;host.innerHTML=`<section class="weather-v2 weather-v2-mobile"><div class="weather-v2-mobile-top"><strong>${weatherIcon(d.code)} ${d.temperature}°</strong><span>${weatherLabel(d.code)}</span></div><div class="weather-v2-mobile-meta"><span>${d.high}° / ${d.low}°</span><span>雨 ${d.rain}%</span></div><button class="wd-card-cta" type="button" data-open-weather aria-label="展開天氣">展開</button></section>`;const button=host.querySelector('[data-open-weather]');button?.addEventListener('click',sdk.openApp);return()=>button?.removeEventListener('click',sdk.openApp)}
