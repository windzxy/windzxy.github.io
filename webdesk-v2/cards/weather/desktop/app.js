import {loadWeather,weatherIcon,weatherLabel,weatherInsight,searchCities,savedLocations,saveLocation} from '../shared/weather-service.js';
export async function mount({host,signal}){
  let active=null;
  async function render(location=null){active=location;host.innerHTML='<section class="weather-app weather-app-desktop" aria-busy="true"></section>';const d=await loadWeather({signal,location});if(signal.aborted)return;const insight=weatherInsight(d);host.innerHTML=`<section class="weather-app weather-app-desktop"><div class="weather-location-bar"><button class="ios-button ios-button-tinted" data-city>${d.place}</button><small>${d.source?`${String(d.updatedAt||'').slice(-5)} · ${d.source}`:'選擇城市取得即時資料'}</small></div><div class="weather-app-hero"><div><h2>${weatherIcon(d.code)} ${d.temperature}°</h2><p>${weatherLabel(d.code)} · 體感 ${d.feelsLike}°</p></div><div class="weather-app-meta"><span>${d.high}° / ${d.low}°</span><span>降雨 ${d.rain}%</span><span>風 ${d.wind} km/h</span><span>陣風 ${d.gust} km/h</span><span>濕度 ${d.humidity}%</span><span>UV ${d.uv}</span></div></div>${insight?`<div class="weather-app-insight"><p>${insight}</p></div>`:''}<div class="weather-app-hourly" aria-label="逐小時預報">${d.hourly.slice(0,8).map(x=>`<article><small>${x.time}</small><strong>${weatherIcon(x.code)}</strong><span>${x.temp}°</span><em>${x.rain}%</em></article>`).join('')}</div><div class="weather-app-forecast" aria-label="七日預報">${d.forecast.slice(0,7).map(x=>`<article><small>${x.label}</small><strong>${weatherIcon(x.code)}</strong><span>${x.high}° / ${x.low}°</span></article>`).join('')}</div><div class="weather-app-details"><span>日出 ${d.sunrise}</span><span>日落 ${d.sunset}</span></div><div class="weather-city-sheet" data-sheet hidden><div class="weather-city-search"><input class="wd-search" data-query type="search" placeholder="搜尋城市" autocomplete="off"><button class="ios-button ios-button-secondary" data-close>完成</button></div><div data-results></div></div></section>`;bind()}
  function bind(){const sheet=host.querySelector('[data-sheet]'),results=host.querySelector('[data-results]'),input=host.querySelector('[data-query]');host.querySelector('[data-city]').onclick=()=>{sheet.hidden=false;showSaved()};host.querySelector('[data-close]').onclick=()=>sheet.hidden=true;let timer;input.oninput=()=>{clearTimeout(timer);timer=setTimeout(async()=>{try{const list=await searchCities(input.value,{signal});paint(list)}catch{}},250)};function paint(list){results.innerHTML=list.map(x=>`<button class="ios-button ios-button-row" data-id="${x.id}"><span>${x.name}</span><small>${x.region}</small></button>`).join('');results.querySelectorAll('[data-id]').forEach((b,i)=>b.onclick=()=>{const loc=list[i];saveLocation(loc);render(loc)})}function showSaved(){paint(savedLocations())}}
  await render(active);
}
