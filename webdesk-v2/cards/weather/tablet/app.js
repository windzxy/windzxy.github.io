import {loadWeather,weatherIcon,weatherLabel} from '../shared/weather-service.js';
export async function mount({host,signal}){host.innerHTML='<section class="weather-app weather-app-tablet"><p>載入天氣中…</p></section>';const d=await loadWeather({signal});if(signal.aborted)return;host.innerHTML=`<section class="weather-app weather-app-tablet"><header><div><small>${d.place}</small><h2>${weatherIcon(d.code)} ${d.temperature}°</h2><p>${weatherLabel(d.code)} · 體感 ${d.feelsLike}°</p></div><div class="weather-tablet-summary"><span>${d.high}° / ${d.low}°</span><span>降雨 ${d.rain}%</span><span>風 ${d.wind} km/h</span></div></header><div class="weather-tablet-days">${d.forecast.slice(0,4).map(x=>`<article><span>${x.label}</span><strong>${weatherIcon(x.code)}</strong><b>${x.high}°</b><small>${x.low}°</small></article>`).join('')}</div></section>`}
