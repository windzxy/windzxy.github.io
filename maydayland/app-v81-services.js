(() => {
  'use strict';
  const VERSION='51.0.0';
  const DATA_URL='./data/source-registry-v51.json?v=51.0.0';
  const $=(s,r=document)=>r.querySelector(s);
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  let data=null, open=false, query='';
  document.addEventListener('DOMContentLoaded',init);
  async function init(){data=await fetch(DATA_URL,{cache:'no-store'}).then(r=>r.ok?r.json():null).catch(()=>null);if(!data)return;injectGlobalUI();document.addEventListener('click',onClick,true);document.addEventListener('keydown',onKey,true);window.addEventListener('hashchange',renderEntityRail);new MutationObserver(()=>requestAnimationFrame(renderEntityRail)).observe(document.body,{subtree:true,childList:true});renderEntityRail();window.dispatchEvent(new CustomEvent('maydayland:services-ready',{detail:{entityNetwork:true}}));}
  function injectGlobalUI(){document.querySelector('#v50SearchLauncher')?.remove();document.querySelector('#v50TrustBadge')?.remove();document.querySelector('#v50Overlay')?.remove();if(!document.querySelector('#v51SearchLauncher')){const b=document.createElement('button');b.id='v51SearchLauncher';b.className='v51-search-launcher';b.type='button';b.innerHTML='<span>⌕</span><b>Search Entity Network</b><small>⌘ K</small>';document.body.appendChild(b);}if(!document.querySelector('#v51TrustBadge')){const badge=document.createElement('div');badge.id='v51TrustBadge';badge.className='v51-trust-badge';badge.innerHTML=`<i></i><span>Entity network · ${data.index.length} nodes</span><b>v81</b>`;document.body.appendChild(badge);}}
  function onKey(e){if((e.metaKey||e.ctrlKey)&&String(e.key).toLowerCase()==='k'){e.preventDefault();toggle(true);}if(e.key==='Escape'&&open)toggle(false);}
  function onClick(e){if(e.target.closest('#v51SearchLauncher')){toggle(true);return;}if(e.target.closest('[data-v51-close]')){toggle(false);return;}const target=e.target.closest('[data-v51-target]');if(target){navigate(target.dataset.v51Target);toggle(false);e.preventDefault();return;}}
  function navigate(target){if(!target)return;const next=target.startsWith('#')?target:'#'+target;if(location.hash===next){window.dispatchEvent(new HashChangeEvent('hashchange'));}else location.hash=next;}
  function toggle(next){open=typeof next==='boolean'?next:!open;renderOverlay();}
  function searchIndex(q){const s=q.trim().toLowerCase();if(!s)return data.index.slice(0,14);return data.index.map(x=>{const hay=[x.title,x.subtitle,x.type,x.page,...(x.keywords||[])].join(' ').toLowerCase();let score=0;if(x.title.toLowerCase()===s)score+=120;if(x.title.toLowerCase().startsWith(s))score+=75;if(x.title.toLowerCase().includes(s))score+=55;if(hay.includes(s))score+=30;for(const part of s.split(/\s+/))if(part&&hay.includes(part))score+=7;return{x,score};}).filter(o=>o.score>0).sort((a,b)=>b.score-a.score||a.x.title.localeCompare(b.x.title)).map(o=>o.x).slice(0,18);}
  function renderOverlay(){let root=document.querySelector('#v51Overlay');if(!open){root?.remove();return;}if(!root){root=document.createElement('div');root.id='v51Overlay';document.body.appendChild(root);}root.className='v51-overlay';root.innerHTML=`<div class="v51-backdrop" data-v51-close></div><section class="v51-command"><header><div><small>ENTITY NETWORK · v81</small><h2>搜尋 Maydayland</h2></div><button data-v51-close>ESC</button></header><div class="v51-input-wrap"><span>⌕</span><input id="v51SearchInput" autocomplete="off" placeholder="城市、專輯、歌曲、巡演、出版物、年代…" value="${esc(query)}"><kbd>⌘ K</kbd></div><div class="v51-command-body"><aside><small>SOURCE TRUST</small>${data.levels.map(l=>`<div class="v51-level v51-${esc(l.id)}"><b>${esc(l.label)}</b><span>${esc(l.score)}%</span><p>${esc(l.note)}</p></div>`).join('')}<div class="v51-network-stat"><b>${data.index.length}</b><span>indexed entities</span><b>${data.relations.length}</b><span>cross-links</span></div></aside><main id="v51Results">${renderResults(searchIndex(query))}</main></div><footer><span>${esc(data.principle)}</span><b>${data.index.length} nodes · ${data.relations.length} links</b></footer></section>`;const input=document.querySelector('#v51SearchInput');input?.focus();input?.setSelectionRange(query.length,query.length);input?.addEventListener('input',e=>{query=e.target.value;const box=document.querySelector('#v51Results');if(box)box.innerHTML=renderResults(searchIndex(query));});}
  function renderResults(items){if(!items.length)return '<div class="v51-empty"><b>沒有匹配項目</b><span>試試「台北」、「自傳」、「倔強」、「諾亞方舟」或「2011」。</span></div>';return `<div class="v51-result-list">${items.map(x=>{const level=data.levels.find(l=>l.id===x.confidence);const source=data.sources.find(s=>s.id===x.sourceId);const count=relationCount(x.id);return `<button class="v51-result" data-v51-target="${esc(x.target)}"><div class="v51-result-icon">${icon(x.type)}</div><div class="v51-result-copy"><small>${esc(x.type.toUpperCase())} · ${esc(x.page)}</small><h3>${esc(x.title)}</h3><p>${esc(x.subtitle)}</p></div><div class="v51-result-trust v51-${esc(x.confidence)}"><b>${esc(level?.label||x.confidence)}</b><span>${count} links · ${esc(source?.name||'source pending')}</span></div><i>↗</i></button>`;}).join('')}</div>`;}
  function currentEntity(){const h=location.hash||'#home';let exact=data.index.find(x=>x.target===h);if(exact)return exact;const page=h.replace(/^#/,'').split('/')[0]||'home';return data.index.find(x=>x.page===page)||null;}
  function relationsFor(id){return data.relations.filter(r=>r.from===id||r.to===id).map(r=>({rel:r,node:data.index.find(x=>x.id===(r.from===id?r.to:r.from))})).filter(x=>x.node);}
  function relationCount(id){return data.relations.filter(r=>r.from===id||r.to===id).length;}
  function renderEntityRail(){if(!data)return;let rail=document.querySelector('#v51EntityRail');const entity=currentEntity();if(!entity){rail?.remove();return;}const rels=relationsFor(entity.id).slice(0,6);if(!rels.length){rail?.remove();return;}if(!rail){rail=document.createElement('aside');rail.id='v51EntityRail';document.body.appendChild(rail);}rail.className='v51-entity-rail';rail.innerHTML=`<div class="v51-rail-head"><span>${icon(entity.type)}</span><div><small>RELATED ENTITIES</small><b>${esc(entity.title)}</b></div><em>${rels.length} links</em></div><div class="v51-rail-links">${rels.map(({rel,node})=>`<button data-v51-target="${esc(node.target)}"><span>${icon(node.type)}</span><div><small>${esc(rel.label)} · ${esc(node.type)}</small><b>${esc(node.title)}</b></div><i>↗</i></button>`).join('')}</div>`;}
  function icon(type){return({city:'城',album:'CD',song:'♪',tour:'線',publication:'書',timeline:'年'})[type]||'·';}
})();

(() => {
  'use strict';
  const DATA_URL='./data/media-layer-v52.json?v=52.0.0';
  const $=(s,r=document)=>r.querySelector(s);
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  let data=null,collapsed=false;
  document.addEventListener('DOMContentLoaded',init);
  async function init(){data=await fetch(DATA_URL,{cache:'no-store'}).then(r=>r.ok?r.json():null).catch(()=>null);if(!data)return;window.addEventListener('hashchange',render);new MutationObserver(()=>requestAnimationFrame(render)).observe(document.body,{subtree:true,childList:true});render();document.body.dataset.v81MediaRail='ready';window.dispatchEvent(new CustomEvent('maydayland:services-ready',{detail:{mediaRail:true}}));}
  function currentPage(){return(location.hash||'#home').replace(/^#/,'').split('/')[0]||'home';}
  function svg(item){const label=esc(item.title).replace(/&/g,'&amp;');const sub=esc(item.subtitle).replace(/&/g,'&amp;');const tone=item.tone||'#31d7ff';const raw=`<svg xmlns="http://www.w3.org/2000/svg" width="960" height="560" viewBox="0 0 960 560"><defs><linearGradient id="g" x1="0" y1="0" x2="1" y2="1"><stop stop-color="#07101e"/><stop offset=".55" stop-color="${tone}" stop-opacity=".42"/><stop offset="1" stop-color="#02050b"/></linearGradient><radialGradient id="r"><stop stop-color="${tone}" stop-opacity=".5"/><stop offset="1" stop-color="${tone}" stop-opacity="0"/></radialGradient></defs><rect width="960" height="560" fill="url(#g)"/><circle cx="720" cy="130" r="260" fill="url(#r)"/><g fill="none" stroke="${tone}" stroke-opacity=".3"><path d="M0 390 C180 310 320 440 480 350 S760 220 960 320" stroke-width="4"/><path d="M0 430 C220 330 350 480 520 380 S760 260 960 350"/></g><g fill="#fff" opacity=".16"><rect x="74" y="90" width="190" height="190" rx="24"/><rect x="286" y="90" width="72" height="190" rx="24"/><rect x="74" y="300" width="284" height="26" rx="13"/></g><text x="74" y="392" fill="#fff" font-family="Arial,Helvetica,sans-serif" font-size="40" font-weight="700">${label}</text><text x="74" y="430" fill="#c8d6ea" font-family="Arial,Helvetica,sans-serif" font-size="20">${sub}</text><text x="74" y="500" fill="#8ea8c7" font-family="Arial,Helvetica,sans-serif" font-size="14" letter-spacing="3">MAYDAYLAND · RIGHTS-SAFE GENERATED VISUAL</text></svg>`;return'data:image/svg+xml;charset=UTF-8,'+encodeURIComponent(raw);}
  function render(){if(!data)return;const page=currentPage();const items=data.items.filter(x=>x.page===page).slice(0,4);let root=$('#v52MediaDock');if(!items.length){root?.remove();return;}if(!root){root=document.createElement('aside');root.id='v52MediaDock';document.body.appendChild(root);}root.className='v52-media-dock'+(collapsed?' v52-hidden':'');root.innerHTML=`<div class="v52-media-shell"><div class="v52-media-head"><div><small>VISUAL & MEDIA LAYER · v81</small><b>${pageLabel(page)} · rights-safe media rail</b></div><button id="v52Toggle">${collapsed?'展開':'收起'}</button></div><div class="v52-media-grid">${items.map(x=>`<article class="v52-media-card"><div class="v52-media-thumb v52-lazy" data-src="${esc(svg(x))}"></div><span class="v52-rights">GENERATED · SAFE</span><div class="v52-media-copy"><small>${esc(x.kind)}</small><b>${esc(x.title)}</b><span>${esc(x.subtitle)}</span></div></article>`).join('')}</div></div>`;$('#v52Toggle',root)?.addEventListener('click',()=>{collapsed=!collapsed;render();});lazyLoad(root);}
  function lazyLoad(root){const nodes=[...root.querySelectorAll('[data-src]')];if(!('IntersectionObserver'in window)){nodes.forEach(load);return;}const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){load(e.target);io.unobserve(e.target);}}),{rootMargin:'120px'});nodes.forEach(n=>io.observe(n));}
  function load(node){node.style.backgroundImage=`url("${node.dataset.src}")`;node.classList.add('is-ready');}
  function pageLabel(p){return({city:'City Dossier',album:'Album Room',books:'Books & Publications',timeline:'Timeline / Archive'})[p]||'Maydayland';}
})();