name: Upgrade board games side choice and mobile dock (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporarily disabled after mobile regression on 2026-09-12."
