name: Load Flight Chess Product V5
on:
  push:
    branches: [master]
    paths:
      - 'Html_tools/flight-chess-product-v5.js'
      - '.github/workflows/load-flight-chess-product-v5.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  inject:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - name: Inject product v5 runtime
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          s=re.sub(r'\n?<script src="Html_tools/flight-chess-product-v5\.js\?v=[^"]+"></script>','',s)
          tag='<script src="Html_tools/flight-chess-product-v5.js?v=20260913-flight-chess-product-v5.0-boardgame"></script>'
          s=s.replace('</body>',tag+'\n</body>')
          p.write_text(s,encoding='utf-8')
          PY
      - name: Commit loader
        run: |
          if git diff --quiet -- index.html; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Load Flight Chess product-grade v5 presentation'
          git push origin master
